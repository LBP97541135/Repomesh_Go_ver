方案表数 44；检查项 6；失败 52
FAIL 映射表出现方案之外的表：['feature_settings']
FAIL 表 `organizations` 的映射既没有端点也没有标为内部表：'`/api/organizations/current`'
FAIL 表 `users` 的映射既没有端点也没有标为内部表：'`/api/auth/github/*`（现行登录）、`/api/auth/attempts/{id}`、`/api/auth/logout`、`/api/session`；`/api/sessions`、`/api/sessions/current`、`/api/users`、`/api/users/{userId}`、`/api/users/{userId}/password`（作废设计）'
FAIL 表 `agents` 的映射既没有端点也没有标为内部表：'`/api/agents`、`/api/agents/{agentId}`'
FAIL 表 `credentials` 的映射既没有端点也没有标为内部表：'`/api/credentials`、`/api/credentials/{key}`'
FAIL 表 `projects` 的映射既没有端点也没有标为内部表：'`/api/projects`、`/api/projects/{projectId}`'
FAIL 表 `repositories` 的映射既没有端点也没有标为内部表：'`/api/repositories`、`/api/repositories/{repositoryId}`、`/api/repositories/{repositoryId}/profile`'
FAIL 表 `plans` 的映射既没有端点也没有标为内部表：'`/api/projects/{projectId}/plans`、`/api/plans/{planId}`、`/api/plans/{planId}/revisions`、`/api/plans/{planId}/apply`'
FAIL 表 `agent_teams` 的映射既没有端点也没有标为内部表：'`/api/projects/{projectId}/agent-teams`、`/api/agent-teams/{teamId}`、`/api/agent-teams/{teamId}/runtime`'
FAIL 表 `review_requests` 的映射既没有端点也没有标为内部表：'`/api/review-requests`、`/api/review-requests/{requestId}`、`/api/review-requests/{requestId}/decision`'
FAIL 表 `tasks` 的映射既没有端点也没有标为内部表：'`/api/projects/{projectId}/tasks`、`/api/tasks/{taskId}`、`/api/tasks/{taskId}/subtasks`、`/api/tasks/{taskId}/reserve`、`/api/tasks/{taskId}/release`、`/api/tasks/{taskId}/pause`、`/api/tasks/{taskId}/resume`、`/api/tasks/{taskId}/cancel`、`/api/tasks/{taskId}/complete`'
FAIL 表 `plan_steps` 的映射既没有端点也没有标为内部表：'`/api/plans/{planId}/steps`、`/api/plan-steps/{stepId}`'
FAIL 表 `task_assignments` 的映射既没有端点也没有标为内部表：'`/api/tasks/{taskId}/assignments`、`/api/task-assignments/{assignmentId}`、`/api/task-assignments/{assignmentId}/result`'
FAIL 表 `handoffs` 的映射既没有端点也没有标为内部表：'`/api/tasks/{taskId}/handoffs`、`/api/handoffs/{handoffId}`、`/api/handoffs/{handoffId}/status`'
FAIL 表 `messages` 的映射既没有端点也没有标为内部表：'`/api/messages`、`/api/messages/{messageId}`'
FAIL 表 `context_objects` 的映射既没有端点也没有标为内部表：'`/api/projects/{projectId}/context-objects`、`/api/context-objects/{objectId}`、`/api/context-objects/{objectId}/content`、`/api/context-objects/{objectId}/versions`'
FAIL 表 `context_bundles` 的映射既没有端点也没有标为内部表：'`/api/task-assignments/{assignmentId}/context-bundles`、`/api/context-bundles/{bundleId}`'
FAIL 表 `context_deltas` 的映射既没有端点也没有标为内部表：'`/api/context-objects/{objectId}/deltas`、`/api/context-deltas/{deltaId}/sync`'
FAIL 表 `context_access_events` 的映射既没有端点也没有标为内部表：'`/api/context-objects/{objectId}/access-events`'
FAIL 表 `change_sets` 的映射既没有端点也没有标为内部表：'`/api/tasks/{taskId}/change-sets`、`/api/change-sets/{changeSetId}`、`/api/change-sets/{changeSetId}/deliver`、`/api/change-sets/{changeSetId}/conflict`、`/api/change-sets/{changeSetId}/archive`'
FAIL 表 `scm_commands` 的映射既没有端点也没有标为内部表：'`/api/change-sets/{changeSetId}/scm-commands`、`/api/scm-commands/{commandId}`、`/api/scm-commands/{commandId}/retry`'
FAIL 表 `scm_observations` 的映射既没有端点也没有标为内部表：'`/api/change-sets/{changeSetId}/scm-observations`'
FAIL 表 `delivery_policies` 的映射既没有端点也没有标为内部表：'`/api/projects/{projectId}/delivery-policy`'
FAIL 表 `validation_snapshots` 的映射既没有端点也没有标为内部表：'`/api/projects/{projectId}/validation-snapshots`、`/api/validation-snapshots/{snapshotId}`'
FAIL 表 `database_branch_validations` 的映射既没有端点也没有标为内部表：'`/api/projects/{projectId}/database-branch-validations`、`/api/database-branch-validations/{validationId}`、`/api/database-branch-validations/{validationId}/cleanup`'
FAIL 表 `skills` 的映射既没有端点也没有标为内部表：'`/api/skills`、`/api/skills/{skillId}`'
FAIL 表 `skill_versions` 的映射既没有端点也没有标为内部表：'`/api/skills/{skillId}/versions`、`/api/skill-versions/{versionId}`、`/api/skill-versions/{versionId}/status`'
FAIL 表 `skill_test_questions` 的映射既没有端点也没有标为内部表：'`/api/skills/{skillId}/test-questions`'
FAIL 表 `skill_evaluation_runs` 的映射既没有端点也没有标为内部表：'`/api/skill-versions/{versionId}/evaluation-runs`、`/api/skill-evaluation-runs/{runId}/judgement`'
FAIL 表 `skill_approvals` 的映射既没有端点也没有标为内部表：'`/api/skill-versions/{versionId}/approval`、`/api/skill-approvals/{approvalId}/decision`'
FAIL 表 `agent_skill_bindings` 的映射既没有端点也没有标为内部表：'`/api/agents/{agentId}/skill-bindings`、`/api/agent-skill-bindings/{bindingId}`'
FAIL 表 `skill_update_suggestions` 的映射既没有端点也没有标为内部表：'`/api/skills/{skillId}/update-suggestions`、`/api/skill-update-suggestions/{suggestionId}/decision`'
FAIL 表 `events` 的映射既没有端点也没有标为内部表：'`/api/events`、`/api/events/{eventId}`、`/api/events/stream`'
FAIL 表 `llm_usage` 的映射既没有端点也没有标为内部表：'`/api/llm-usage`、`/api/llm-usage/summary`'
FAIL 表 `alert_rules` 的映射既没有端点也没有标为内部表：'`/api/alert-rules`、`/api/alert-rules/{ruleId}`'
FAIL 表 `alert_events` 的映射既没有端点也没有标为内部表：'`/api/alert-events`、`/api/alert-events/{eventId}`、`/api/alert-events/{eventId}/claim`、`/api/alert-events/{eventId}/resolve`'
FAIL 表 `trace_sessions` 的映射既没有端点也没有标为内部表：'`/api/trace-sessions`、`/api/trace-sessions/{sessionId}`'
FAIL 表 `trace_events` 的映射既没有端点也没有标为内部表：'`/api/trace-sessions/{sessionId}/events`'
FAIL 表 `log_entries` 的映射既没有端点也没有标为内部表：'`/api/log-entries`'
FAIL 表 `recovery_cases` 的映射既没有端点也没有标为内部表：'`/api/recovery-cases`、`/api/recovery-cases/{caseId}`、`/api/recovery-cases/{caseId}/close`'
FAIL 表 `recovery_decisions` 的映射既没有端点也没有标为内部表：'`/api/recovery-cases/{caseId}/decisions`'
FAIL 表 `recovery_operations` 的映射既没有端点也没有标为内部表：'`/api/recovery-cases/{caseId}/operations`'
FAIL 表 `decision_chain_nodes` 的映射既没有端点也没有标为内部表：'`/api/decision-chains`、`/api/decision-chains/{nodeId}`、`/api/decision-chains/similar`、`/api/settings/decision-chain`'
FAIL 表 `feature_settings` 的映射既没有端点也没有标为内部表：'`/api/settings/decision-chain`、`/api/settings/skill-governance`'
FAIL 表 `decision_embeddings` 的映射既没有端点也没有标为内部表：'`/api/decision-chains/semantic-search`、`/api/decision-chains/embeddings/refresh`'
FAIL 表 `mcp_server_policies` 的映射既没有端点也没有标为内部表：'`/api/mcp-server-policies`、`/api/mcp-server-policies/{serverName}`'
FAIL 正文端点数量只有 5，低于 40，疑似格式不符
FAIL 端点 /api/v1/review-requests 的顶层资源 `review-requests` 未出现在映射表的端点列中
FAIL 端点 /api/v1/review-requests/events 的顶层资源 `review-requests` 未出现在映射表的端点列中
FAIL 端点 /api/v1/projects/{param}/checkpoint-decisions 的顶层资源 `projects` 未出现在映射表的端点列中
FAIL 端点 /api/v1/projects/{param}/control 的顶层资源 `projects` 未出现在映射表的端点列中
FAIL 端点 /api/v1/projects/{projectId}/checkpoint-decisions 的顶层资源 `projects` 未出现在映射表的端点列中
