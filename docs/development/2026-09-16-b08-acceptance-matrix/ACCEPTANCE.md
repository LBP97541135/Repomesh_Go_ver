# B08 验收文档：管理闭环验收矩阵设计（任务 #14）

对应 ASTRA 文档 B08（验收设计批次）。分支 feat/astra-b05。

## 需求是什么

B08 是验收设计批次（ASTRA 分工："Astra 验收设计，Sol 后续执行"）：
1. 跨批验收矩阵：登录、项目、配置、建项、查看、编辑全流程用例。
2. 故障注入点：HTTP/事务/锁/外部依赖/进程生命周期五层。
3. 证据标准与发布条件：每类检查的留证要求、INTEGRATED_LOCAL_VERIFIED 的达成条件。
4. 明确"设计完成不勾选实际 PASS"（IMPLEMENTATION-PLAN B08 行）。

## 做了什么

- 产出 docs/development/2026-09-16-b08-acceptance-matrix/ACCEPTANCE-MATRIX.md（约 160 行），六节：
  1. 批次覆盖表（B02—B07 各批验收入口挂接，B09+ 仅记录未接入）。
  2. 全流程用例矩阵：A（登录 4 例）/ P（项目 5 例）/ M1（模型来源 4 例）/ M2（测试应用 5 例）/ I（Issue 创建 7 例）/ Q（查看 6 例）/ E（编辑恢复 4 例），共 35 条用例，每条含前提/动作/预期/证据类别。
  3. 故障注入点 5 层清单（HTTP、事务 kill、幂等并发、外部断网、进程 SIGKILL）。
  4. 证据标准（五类证据）+ 发布条件 5 条 + 明确不 PASS 项。
  5. 与 DEVELOPMENT-START 失败验收要求的逐条映射表（9 行）。
  6. 执行注意（沿用 b04-closeout-01 格式，失败轮保留）。

## 影响范围

- 纯设计文档，不修改产品代码、迁移或运行配置（遵守 ASTRA B08 排除项）。
- 后续 B08 实际执行时按矩阵逐项留证到 docs/development/<日期>-b08-closeout-01/。

## 修复逻辑（关键设计决策）

1. 用例编号按批次分组而非按页面分组：每条用例能直接回链到产出批次的验收文档（B05/B06/B07 ACCEPTANCE.md），失败时归批修复，不在 B08 就地补功能。
2. rooms 运行状态单列"明确不 PASS"：B07 的 unavailable 基线是诚实状态，B08 执行时禁止把"未接入"包装成通过。
3. 发布条件显式包含 businessReady=false 与 /readyz 503——与 B04 收口的"集成验证≠整批业务 VERIFIED"边界一致。

## 偏差清单

| # | 规格声明 | 实际交付 | 处理 |
|---|---------|---------|------|
| 1 | "故障和恢复矩阵"含 SSE 断线恢复 | 矩阵 Q 组仅含 REST 面 + 前端代次 | SSE 未实现（B07 偏差 #3），执行轮把 SSE 用例标 DEFERRED 而非伪造 |
| 2 | "完整工程检查及打包" | 矩阵第四节列命令清单，本轮未执行 | B08 执行轮职责；设计轮不预跑（ASTRA 排除项） |

## 对应 commit

- docs(b08): acceptance matrix design（本文件同批提交）

## 验证结果

- 纯文档批次；go build/vet/test 不受影响（合并前统一复跑）。
